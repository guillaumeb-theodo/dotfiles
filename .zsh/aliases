for d in $HOME/.zsh/aliases.d/*; do
    source $d;
done

alias hist='history | grep'
alias mine='sudo chown -R nadrieril:nadrieril'
alias findfile='sudo find / -name'

alias getip='wget -qO- http://ipecho.net/plain ; echo'
alias explore='nautilus "$PWD"'

alias n='nano'
alias e='editor'

